============
Installation
============

At the command line::

    $ easy_install xmljson

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv xmljson
    $ pip install xmljson
